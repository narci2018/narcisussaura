<!-- Android Permissions and VpnService snippet for Narcissus Aura -->
<manifest-additions>
    <permissions>
        <uses-permission android:name="android.permission.INTERNET" />
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
        <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
        <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
        <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
        <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    </permissions>
    <application>
        <provider
            android:name=".VpnInitProvider"
            android:authorities="${applicationId}.vpninitprovider"
            android:exported="false"
            android:initOrder="100" />
    </application>
    <services>
        <service
            android:name=".NarcissusVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:enabled="true"
            android:exported="false"
            android:label="Narcissus Aura"
            android:foregroundServiceType="specialUse">
            <!-- REQUIRED for the app to appear in Settings -> VPN (MIUI lists
                 only VpnService declarations with this metadata); that page is
                 the consent entry point MIUI cannot swallow. -->
            <meta-data
                android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"
                android:value="true" />
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="VPN proxy connection and traffic routing" />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>
    </services>
</manifest-additions>
